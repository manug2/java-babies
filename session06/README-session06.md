# java-babies Session 06

Tasks for this session
- Create an interface `TimeProvider` to `get()` a `String` representing time.
- Create a class implementing `TimeProvider` which returns current time in "HH:MM" (hour and minute) format.
- Create a class implementing `TimeProvider` which returns current time with date, time and time zone details.
